#!/bin/sh

#
# Warm cache for build and test processes
#

DOCKER=$(which docker)

if [ DOCKER = '' ]; then
    echo 'Docker is required\n'
    exit 1
fi

echo "#\n# Warming cache\n#\n"
for image in "$BUILD_FROM"
do 
    docker pull ${image};
done

exit 0