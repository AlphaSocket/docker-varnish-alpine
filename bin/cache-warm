#!/bin/sh

#
# Warm cache for build and test processes
#

DOCKER=$(which docker)

if [ DOCKER = '' ]; then
    echo 'Docker is required\n'
    exit 1
fi

echo "#\n# Warming cache\n#\n"
for image in "alpine:latest 03192859189254/dockerized-nginx-tester:latest"
do 
    docker pull ${image} 1>/dev/null;
done

exit 0