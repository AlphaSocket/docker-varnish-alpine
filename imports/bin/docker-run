#!/bin/sh
#
# Run command
#
set -e

# Configuration
$(which docker-config)

echo "### Starting container service"
sh -c "varnishd $CONFIG_VARNISH_STARTUP_OPTIONS $CONFIG_VARNISH_STARTUP_ADDITIONAL_OPTIONS"

# Container command is failed if exiting here
exit $?